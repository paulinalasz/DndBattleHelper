<Window x:Class="DndBattleHelper.Views.EditEnemyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DndBattleHelper.Views"
             Title="Edit Enemy"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Height="560" Width="870" MinWidth="870" MinHeight="560">
    <Grid Background="{DynamicResource DndBeige}">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <local:NewEnemyView Grid.Row="0"/>
        <StackPanel Grid.Row="1" VerticalAlignment="Bottom" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button ToolTip="Edit" Command="{Binding AddCommand}" Style="{StaticResource Confirm}" Content="Edit"/>
            <Button Command="{Binding CancelCommand}" Style="{StaticResource Cancel}">
                <Viewbox>
                    <Image Source="{StaticResource CancelDrawingImage}"/>
                </Viewbox>
            </Button>
        </StackPanel>
    </Grid>
</Window>

