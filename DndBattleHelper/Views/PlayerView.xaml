<UserControl x:Class="DndBattleHelper.Views.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DndBattleHelper.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{DynamicResource DndBeige}">
        <StackPanel>
            <TextBlock Style="{StaticResource Name}" Text="{Binding Name}"/>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Style="{StaticResource TraitName}" Text="Initiative: "/>
                <TextBlock Grid.Row="1" Grid.Column="0" Style="{StaticResource TraitName}" Text="Armour Class: "/>
                <TextBox Grid.Row="0" Grid.Column="1" Style="{StaticResource Traits}" Text="{Binding Initiative}"/>
                <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource Traits}" Text="{Binding ArmourClass}"/>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
