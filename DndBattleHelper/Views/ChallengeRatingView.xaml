<UserControl x:Class="DndBattleHelper.Views.ChallengeRatingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DndBattleHelper.Views"
             xmlns:converters="clr-namespace:DndBattleHelper.Views.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:EnumToDescriptionConverter x:Key="enumToDescriptionConverter"/>
    </UserControl.Resources>
    <Grid>
        <StackPanel Orientation="Horizontal">
            <TextBlock Margin="5, 0, 5, 0" Style="{StaticResource TraitName}" Text="Challenge"/>
            <TextBlock Margin="5, 0, 5, 0" Style="{StaticResource Trait}" Text="{Binding SelectedChallengeRating, Converter={StaticResource enumToDescriptionConverter}}"/>
            <TextBlock Style="{StaticResource Trait}">
                <Run Text="{Binding Xp, Mode=OneWay}"/>
                <Run Text="Xp"/>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
