<UserControl x:Class="DndBattleHelper.Views.TraitViews.TraitsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DndBattleHelper.Views.TraitViews"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <Grid>
        <StackPanel Orientation="Horizontal" Visibility="{Binding IsVisible, Converter={StaticResource BoolToVis}}">
            <TextBlock  Margin="5, 0, 5, 0" Style="{StaticResource TraitName}" Text="{Binding Header}"/>
            <TextBlock TextWrapping="Wrap" MaxWidth="260" Margin="5, 0, 5, 0" Style="{StaticResource Trait}" Text="{Binding }"/>
        </StackPanel>
    </Grid>
</UserControl>
